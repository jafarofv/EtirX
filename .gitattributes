# Normalize line endings to LF so Prettier --check and CI are deterministic
# across platforms (Windows checkouts won't flip source files to CRLF).
*.ts   text eol=lf
*.tsx  text eol=lf
*.css  text eol=lf
*.js   text eol=lf
*.json text eol=lf
*.md   text eol=lf
